---
apiVersion: v1
kind: Service
metadata:
  name: mizutest-outbound-tls-openssl-randomuser
  annotations:
        prometheus.io/scrape: 'true'
  labels:
    name: mizutest-outbound-tls-openssl-randomuser
  namespace: sock-shop
spec:
  ports:
    # the port that this service should serve on
  - port: 80
    targetPort: 80
  selector:
    name: mizutest-outbound-tls-openssl-randomuser
