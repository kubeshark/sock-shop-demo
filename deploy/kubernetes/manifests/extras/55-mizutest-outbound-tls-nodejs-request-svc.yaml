---
apiVersion: v1
kind: Service
metadata:
  name: mizutest-outbound-tls-nodejs-request
  annotations:
        prometheus.io/scrape: 'true'
  labels:
    name: mizutest-outbound-tls-nodejs-request
  namespace: sock-shop
spec:
  ports:
    # the port that this service should serve on
  - port: 80
    targetPort: 80
  selector:
    name: mizutest-outbound-tls-nodejs-request
