---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mizutest-inbound-tls-golang-client
  labels:
    name: mizutest-inbound-tls-golang-client
  namespace: sock-shop
spec:
  replicas: 1
  selector:
    matchLabels:
      name: mizutest-inbound-tls-golang-client
  template:
    metadata:
      labels:
        name: mizutest-inbound-tls-golang-client
    spec:
      containers:
      - name: mizutest-inbound-tls-golang-client
        image: mertyildiran/mizutest-inbound-tls-golang-client:latest
        imagePullPolicy: Always
        command: ["./main"]
        resources:
          limits:
            cpu: 50m
            memory: 200Mi
          requests:
            cpu: 10m
            memory: 10Mi
        ports:
        - containerPort: 80
        # securityContext:
        #   runAsNonRoot: true
        #   runAsUser: 10001
        #   capabilities:
        #     drop:
        #       - all
        #     add:
        #       - NET_BIND_SERVICE
        #   readOnlyRootFilesystem: true
      nodeSelector:
        beta.kubernetes.io/os: linux
